# 说明文档
## 运行环境
Ubuntu 20.04及以上

## 编译
```
# 进入项目文件夹

mkdir build && cd build

cmake ..

make
```

## 运行
> 运行前，请打开 src/config.json, ADDRESS_CONFIG的第一个元素必须是 MASTER 节点的真实 IP，端口必须是 9001; 后面的元素是 DataNode 节点的真实 IP，端口从 9002 开始递增

> 本地运行统一改成 127.0.0.1
```
cd build/bin

# 运行第一个数据节点
./lekv 9002

# 运行第二个数据节点
./lekv 9003

# 运行 Master 节点, Master节点一定要在数据节点启动后再启动
./lekv 9001

# 运行客户端进行交互
./lekv_cli
```

## 测试
```
# 先将分布式系统运行起来
./lekv 9002

./lekv 9003

./lekv 9001

# 测试增删改查
python test_crud.py

# 测试分片分裂与负载均衡
python test_split_migrate.py

# 测试性能
python test_perf.py
```

## 日志
在 build/bin/文件夹里
- db_2: LevelDB 存储日志
- db_3: LevelDB 存储日志
- tablets.json: 上次分片路由表的持久化存储
- transfer_time.json: 数据迁移的个数与耗时
